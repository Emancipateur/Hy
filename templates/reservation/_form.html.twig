

 	{% if app.request.attributes.get('_route') == 'app_reservation_new' %}
<div class="suite-js" data-is-suite="{{ suite.id }}"></div>
{% endif %}
{% if app.user %}
<div class="client-js" data-is-client="{{ app.user.id }}"></div>
{% endif %}
 
</div>
{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>


    <div class="row">
        <div class="col">
         	{% if app.request.attributes.get('_route') == 'app_reservation_new' %}
            {{ form_widget(form.suites, {'value' : suite.id}) }}
            {% endif %}
        </div>


    </div>
{{ form_end(form) }}
  <div id="root"></div>
