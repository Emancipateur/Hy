{{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>
{{dump((suite))}}
{{dump((form.suites.vars.data))}}
    <div class="row">
        <div class="col">
            {{ form_widget(form.suites, {'value' : suite.id}) }}
          
        </div>
        <div class="col" id="some-custom-id">
            {{ form_row(form.debut) }}
        </div>
        <button>Clique</button>
    </div>
{{ form_end(form) }}